# To pull changes in workspace
git pull
# Add the files in local repo.
git add .
# Commint changes with comments
git commit -m “comment”
#Push changes in rmemote remo.
git push
#Fetch the changes in local repo.
git fetch
# mergae the conflicts with local repo and remote repo
git merge

===================

#count the object
git count-objects -v
#Identify and Verity the large files
git verify-pack -v .git/objects/pack/pack-7b03cc896f31b2441f3a791ef760bd28495697e6.idx | sort -k 3 -n | tail -10
#Find the file type and update extension accordingly
git filter-branch --index-filter 'git rm --cached --ignore-unmatch *.mov' -- --all
#remove the backup reference
rm -Rf .git/refs/original
#remove the logs
rm -Rf .git/logs/
#remove files not in the db
git gc --aggressive --prune=now
#count the object
git count-objects -v




